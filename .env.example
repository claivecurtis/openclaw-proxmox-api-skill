# Example environment variables for OpenClaw Proxmox API Skill
# Set OPENCLAW_WORKSPACE to the path of your OpenClaw workspace directory
# If not set, defaults to the parent directory of the scripts folder
OPENCLAW_WORKSPACE=/home/user/.openclaw/workspace